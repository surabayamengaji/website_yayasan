import { Component } from '@angular/core';
import { Button } from '../../button/button';

@Component({
  selector: 'app-mid-header',
  imports: [Button],
  templateUrl: './mid-header.html',
  styleUrl: './mid-header.css',
})
export class MidHeader {}
